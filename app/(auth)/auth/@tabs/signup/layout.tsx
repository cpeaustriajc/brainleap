import React from 'react'

export const metadata = {
  title: 'Sign Up | Brainleap',
}

export default function SignUpLayout({
  children,
}: { children: React.ReactNode }) {
  return <React.Fragment>{children}</React.Fragment>
}
